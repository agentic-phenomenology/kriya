services:
  # Backend API
  - type: web
    name: kriya-agents-api
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: OPENROUTER_API_KEY
        sync: false
      - key: AUTH_USERNAME
        sync: false
      - key: AUTH_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://kriya-agents.onrender.com
      - key: OPENCLAW_WEBHOOK_SECRET
        sync: false

  # Frontend
  - type: web
    name: kriya-agents
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://kriya-agents-api.onrender.com
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
